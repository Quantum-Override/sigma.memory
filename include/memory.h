/*
 * SigmaCore — sigma.memory
 * ----------------------------------------------
 * File: memory.h
 * Description: Compatibility shim — redirects bare `#include "memory.h"`
 *              (used by src/memory.c and internal tooling) to the canonical
 *              public header at sigma.memory/memory.h.
 *              Do not add content here.
 */
#pragma once

#include "sigma.memory/memory.h"
